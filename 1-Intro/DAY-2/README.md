DAY-2
- Explicit return on JSX is using 'return' keyword and writing the HTML we need to render.
- Implicity return uses ( ) braces directly and JSX understands that it needs to be rendered.

- Use lists using the conventional JS and XML formatting of JSX/React

- Props in react allow us to pass sme data betwen components.
- Props are always passed FROM PARENT TO CHILD component.
- To recieve, use (props) as the argument while defining the child component.
- 'children' keyword.